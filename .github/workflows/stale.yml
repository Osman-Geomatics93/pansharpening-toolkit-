name: "Close Stale Issues and PRs"

on:
  schedule:
    - cron: '0 0 * * *'  # Run daily at midnight
  workflow_dispatch:  # Allow manual trigger

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
    - name: Close Stale Issues and PRs
      uses: actions/stale@v10
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}

        # Issue settings
        stale-issue-message: |
          This issue has been automatically marked as stale because it has not had recent activity.
          It will be closed in 14 days if no further activity occurs.
          Thank you for your contributions!
        close-issue-message: |
          This issue has been automatically closed due to inactivity.
          Feel free to reopen if you believe this issue still needs attention.
        stale-issue-label: 'stale'
        days-before-issue-stale: 60
        days-before-issue-close: 14
        exempt-issue-labels: 'pinned,security,bug,enhancement,help wanted,good first issue'

        # PR settings
        stale-pr-message: |
          This pull request has been automatically marked as stale because it has not had recent activity.
          It will be closed in 14 days if no further activity occurs.
          Thank you for your contributions!
        close-pr-message: |
          This pull request has been automatically closed due to inactivity.
          Feel free to reopen if you would like to continue working on this.
        stale-pr-label: 'stale'
        days-before-pr-stale: 60
        days-before-pr-close: 14
        exempt-pr-labels: 'pinned,security,work in progress'

        # General settings
        operations-per-run: 100
        remove-stale-when-updated: true
        delete-branch: false
